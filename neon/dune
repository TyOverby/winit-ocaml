(executable
 (name neon)
 (libraries
  winit
  softbuffer
  unix
  bigarray
  core
  core_unix
  core_unix.command_unix
  image_buf
  sdf
  sdf_gpu
  neo
  parallel
  parallel.scheduler)
 (preprocess
  (pps ppx_jane)))
