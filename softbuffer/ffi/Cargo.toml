[package]
name = "softbuffer_ffi"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib", "cdylib", "rlib"]

[dependencies]
softbuffer = { path = "../../vendor/softbuffer" }
winit_ffi = { path = "../../winit/ffi" }
winit = { path = "../../vendor/winit/winit" }
